var firstNumber =  prompt('add the first number');
var secondNumber = prompt('add the second number');

var sum = firstNumber * secondNumber;
var message = '-- Сумма чисел =' + ' ' + sum + ' ' + '--';

document.write(message);
